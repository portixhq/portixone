# Docs

Placeholder. Real content (quickstart, troubleshooting) is Week 1-8 work per the operating manual, as the printing flow becomes real (beyond the current `mock.driver.ts`). The public-facing docs site is [`portix.dev/docs`](https://github.com/portixhq/portix.dev).

For now:
- SDK quickstart → [`sdk-js/README.md`](../sdk-js/README.md)
- Running the runtime → [`runtime/README.md`](../runtime/README.md)
