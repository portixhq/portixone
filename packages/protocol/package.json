{
  "name": "@portixone/protocol",
  "version": "0.1.0",
  "description": "Message contract (print jobs, capabilities, WebSocket events) shared between the Portix Runtime and its SDKs.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/portixhq/portixone.git",
    "directory": "packages/protocol"
  },
  "homepage": "https://github.com/portixhq/portixone/tree/master/packages/protocol#readme",
  "bugs": "https://github.com/portixhq/portixone/issues",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
